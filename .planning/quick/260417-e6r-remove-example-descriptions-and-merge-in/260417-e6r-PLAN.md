---
phase: quick-260417-e6r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+page.svelte
autonomous: true
requirements:
  - QUICK-260417-e6r

must_haves:
  truths:
    - "Hero section has exactly one intro paragraph (1-2 sentences)"
    - "No per-example description paragraphs exist on the page"
    - "Features bullet list is unchanged"
    - "Selected value display lines above each example are unchanged"
    - ".section-description CSS class is removed from the style block"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Cleaned demo page with merged intro and no example descriptions"
  key_links: []
---

<objective>
Remove verbose per-example description paragraphs from the demo page and merge the two hero intro paragraphs into a single concise paragraph.

Purpose: Streamline the demo page by removing redundant prose that clutters the examples.
Output: Updated +page.svelte with cleaner layout.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge hero paragraphs and remove example descriptions</name>
  <files>src/routes/+page.svelte</files>
  <action>
In src/routes/+page.svelte, make these changes:

1. **Merge hero paragraphs**: Replace the two `<p class="hero-description">` elements (lines 257-273) with a single paragraph. The merged text should be 1-2 sentences covering: iOS-style wheel picker for Svelte 5, smooth inertia scrolling, infinite loop, keyboard navigation, zero dependencies, headless/unstyled. Example:

```svelte
<p class="hero-description">
  An iOS-style wheel picker for Svelte 5 — smooth inertia scrolling, infinite loop, cylindrical 3D drum effect, and full keyboard navigation. Headless, zero dependencies, works via npm or the shadcn-svelte CLI.
</p>
```

2. **Remove all per-example description paragraphs**: Delete every `<p class="section-description">...</p>` element. There are 6 of them in these sections:
   - Single Wheel (lines 302-307)
   - Disabled Options (lines 322-327)
   - Infinite Loop (lines 343-348)
   - Two Wheels (lines 363-368)
   - Drum / Cylinder (lines 396-400)
   - Sensitivity (lines 417-422)

3. **Remove .section-description CSS**: Delete the `.section-description` style rule (lines 612-617).

Do NOT remove:
- The Features section or its bullet list
- The "Selected: X" / "Hour: X — Minute: X" paragraph lines above each example
- The slider controls in the Sensitivity section
  </action>
  <verify>
    <automated>cd /Users/instinct/Desktop/working/wheel-picker/svelte-wheel-picker && grep -c "section-description" src/routes/+page.svelte | grep -q "^0$" && echo "PASS: no section-description found" || echo "FAIL: section-description still exists"</automated>
  </verify>
  <done>
- Hero section contains exactly one `<p class="hero-description">` with 1-2 sentences
- Zero occurrences of "section-description" in the file (class removed from both markup and style)
- Features list intact
- "Selected:" display lines intact
- Page builds without errors: `pnpm build` succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this is a static content change to the demo page.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Pure content removal, no security surface |
</threat_model>

<verification>
- `grep -c "section-description" src/routes/+page.svelte` returns 0
- `grep -c "hero-description" src/routes/+page.svelte` returns exactly 2 (one in markup, one in style block)
- `pnpm build` succeeds without errors
</verification>

<success_criteria>
Demo page has a single concise hero paragraph, no per-example descriptions, features list and selected-value lines unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260417-e6r-remove-example-descriptions-and-merge-in/260417-e6r-SUMMARY.md`
</output>
