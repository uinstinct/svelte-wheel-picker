---
phase: quick-260402-d8h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/WheelPicker.svelte
autonomous: true
requirements: [fix-scroll-sensitivity-reactivity]

must_haves:
  truths:
    - "Changing scrollSensitivity prop at runtime visibly changes wheel deceleration behavior"
    - "Changing dragSensitivity prop at runtime visibly changes inertia overshoot"
    - "Changing itemHeight, visibleCount, infinite, options at runtime updates physics accordingly"
  artifacts:
    - path: "src/lib/WheelPicker.svelte"
      provides: "Reactive prop sync to WheelPhysics via $effect"
      contains: "physics.update"
  key_links:
    - from: "src/lib/WheelPicker.svelte"
      to: "WheelPhysics.update()"
      via: "$effect watching props"
      pattern: "physics\\.update"
---

<objective>
Fix scroll sensitivity (and all other physics-related props) not updating at runtime.

Purpose: WheelPhysics is instantiated once in the component script body. The `update()` method exists on WheelPhysics to accept new config values, but no `$effect` in WheelPicker.svelte ever calls it. When a consumer changes `scrollSensitivity` (or `dragSensitivity`, `optionItemHeight`, `visibleCount`, `infinite`, `options`) at runtime, the physics engine keeps using the stale construction-time values.

Output: WheelPicker.svelte with a reactive $effect that syncs prop changes to physics.update().
</objective>

<execution_context>
@/Users/instinct/Desktop/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/WheelPicker.svelte
@src/lib/use-wheel-physics.svelte.ts
@src/lib/wheel-physics-utils.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add $effect to sync props to WheelPhysics.update()</name>
  <files>src/lib/WheelPicker.svelte</files>
  <action>
Add a `$effect` block in WheelPicker.svelte (after the existing physics instantiation, near the other $effect blocks around lines 98-122) that calls `physics.update()` with all configuration props whenever they change.

The effect should read all reactive props so Svelte tracks them:

```typescript
$effect(() => {
  physics.update({
    itemHeight: optionItemHeight,
    visibleCount,
    dragSensitivity,
    scrollSensitivity,
    infinite,
    options,
    onSnap: (index: number) => {
      // same onSnap logic as constructor — keep in sync
    },
  });
});
```

IMPORTANT: The onSnap callback references `infinite` and `options` which are reactive props. Since `physics.update()` stores these as plain fields (not $state), the callback must be re-passed whenever those props change so the closure captures current values. Duplicating the onSnap logic is acceptable here — extract it to a named function if cleaner.

To avoid duplication, extract the onSnap handler into a standalone function `handleSnap(index: number)` defined before both the constructor and the $effect. Both the constructor's `onSnap` and the $effect's `onSnap` should reference this function. The function body should read `infinite` and `options` directly (they are reactive via $props), so it always uses current values.

Also while in this file: remove the leftover `console.log` calls in `onSnap`, `onPointerDown`, and `onPointerUp` — these are debug artifacts from Phase 03.
  </action>
  <verify>
    <automated>cd /Users/instinct/Desktop/wheel-picker/svelte-wheel-picker && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `physics.update()` is called reactively whenever any physics-related prop changes
    - No duplicate onSnap logic (extracted to shared function)
    - Console.log debug statements removed from WheelPicker.svelte
    - TypeScript compilation passes with no errors
  </done>
</task>

</tasks>

<verification>
1. `npx svelte-check` passes with no errors
2. Manual test: open demo site (`pnpm dev`), go to Scroll Sensitivity section, move slider — wheel deceleration should visibly change when flicking
</verification>

<success_criteria>
- Adjusting scrollSensitivity slider in demo produces visibly different deceleration on flick
- No TypeScript errors
- No console.log debug noise in production component
</success_criteria>

<output>
After completion, create `.planning/quick/260402-d8h-fix-scroll-sensitivity-not-changing-visi/260402-d8h-SUMMARY.md`
</output>
