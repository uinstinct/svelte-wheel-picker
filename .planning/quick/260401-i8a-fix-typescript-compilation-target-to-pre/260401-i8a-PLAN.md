---
phase: quick-260401-i8a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "tsconfig.json explicitly sets target to ES2022 or higher, independent of .svelte-kit/tsconfig.json inheritance"
    - "dist output still preserves native #private class fields and $state() rune calls after rebuild"
  artifacts:
    - path: "tsconfig.json"
      provides: "Explicit TS compilation target"
      contains: "target"
  key_links: []
---

<objective>
Make the TypeScript compilation target explicit in tsconfig.json so it is self-documenting and resilient to upstream SvelteKit config changes.

Purpose: The current target ("esnext") is inherited implicitly from .svelte-kit/tsconfig.json. This works correctly today but is fragile -- if SvelteKit changes its generated defaults, the library dist could silently break. Making the target explicit ensures the library always compiles with native class field support regardless of upstream changes.

Output: Updated tsconfig.json with explicit target field, verified dist output.
</objective>

<execution_context>
@/Users/instinct/Desktop/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@tsconfig.json
@.svelte-kit/tsconfig.json

Research confirmed the current effective target is already "esnext" (inherited from .svelte-kit/tsconfig.json).
The dist output already preserves native class fields and $state() runes. This task makes that explicit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit target to tsconfig.json and verify dist output</name>
  <files>tsconfig.json</files>
  <action>
1. Add `"target": "esnext"` to the `compilerOptions` in `tsconfig.json`. Use "esnext" (not "ES2022") because:
   - It matches what the inherited config already provides, so behavior is identical
   - "esnext" always includes the latest features, so it won't need updating as new ES versions release
   - "ES2022" would technically be a downgrade from the current effective "esnext"

2. Add a brief comment above the target field explaining why it is explicit:
   Note: tsconfig.json does not support comments natively, so do NOT add a comment. Instead, the self-documenting nature of having the field present is sufficient.

3. Rebuild the library: `pnpm package`

4. Verify dist output still preserves native class fields by checking:
   - `dist/use-wheel-physics.svelte.js` contains `#itemHeight` (native private field, not WeakMap)
   - `dist/use-controllable-state.svelte.js` contains `#internal = $state` (native private + rune)
   - `dist/use-typeahead-search.svelte.js` contains `#buffer = $state` (native private + rune)

If any file shows WeakMap patterns instead of native private fields, something went wrong -- revert and investigate.
  </action>
  <verify>
    <automated>cd /Users/instinct/Desktop/wheel-picker/svelte-wheel-picker && pnpm package && grep -q '#itemHeight' dist/use-wheel-physics.svelte.js && grep -q '#internal' dist/use-controllable-state.svelte.js && grep -q '#buffer' dist/use-typeahead-search.svelte.js && echo "PASS: dist preserves native class fields"</automated>
  </verify>
  <done>tsconfig.json has explicit "target": "esnext" in compilerOptions. dist output verified to preserve native #private class fields and $state() rune calls.</done>
</task>

</tasks>

<verification>
- `npx tsc --showConfig | grep target` shows "esnext"
- dist/*.svelte.js files contain `#` private fields (not WeakMap)
- dist/*.svelte.js files contain `$state()` calls (not compiled away)
</verification>

<success_criteria>
- tsconfig.json explicitly declares target in compilerOptions
- Library builds successfully with `pnpm package`
- dist output is byte-for-byte equivalent to before the change (since effective target is unchanged)
</success_criteria>

<output>
After completion, create `.planning/quick/260401-i8a-fix-typescript-compilation-target-to-pre/260401-i8a-SUMMARY.md`
</output>
