---
phase: 06-shadcn-registry-and-demo-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - registry.json
  - static/r/wheel-picker.json
  - src/routes/+page.svelte
autonomous: true
requirements:
  - DIST-04
  - DIST-05

must_haves:
  truths:
    - "registry.json contains a single wheel-picker item with all 7 source files"
    - "static/r/wheel-picker.json exists and was generated by registry:build"
    - "Demo page shows a hero section with component title, description, and install commands"
    - "All four existing demo examples remain unchanged and functional"
    - "Hero uses only the four existing CSS custom properties — no new color tokens"
  artifacts:
    - path: "registry.json"
      provides: "shadcn-svelte registry manifest with wheel-picker item"
      contains: "wheel-picker"
    - path: "static/r/wheel-picker.json"
      provides: "Built registry JSON served as static asset"
      contains: "WheelPicker"
    - path: "src/routes/+page.svelte"
      provides: "Demo page with hero section prepended above examples"
      contains: "svelte-wheel-picker"
  key_links:
    - from: "registry.json"
      to: "static/r/wheel-picker.json"
      via: "registry:build command"
      pattern: "wheel-picker"
    - from: "src/routes/+page.svelte"
      to: "static/r/wheel-picker.json"
      via: "shadcn add URL in hero code snippet"
      pattern: "svelte-wheel-picker.vercel.app/r/wheel-picker.json"
---

<objective>
Set up the shadcn-svelte registry and enhance the demo site with a landing page hero section.

Purpose: Complete the two remaining v1 distribution requirements — shadcn CLI integration (DIST-04) and a demo site showcasing the component (DIST-05).
Output: A populated registry.json, a built static/r/wheel-picker.json, and an enhanced +page.svelte with hero section.
</objective>

<execution_context>
@/Users/instinct/Desktop/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/wheel-picker/svelte-wheel-picker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shadcn-registry-and-demo-site/06-CONTEXT.md
@.planning/phases/06-shadcn-registry-and-demo-site/06-RESEARCH.md
@.planning/phases/06-shadcn-registry-and-demo-site/06-UI-SPEC.md

<interfaces>
<!-- Existing registry.json scaffold (items array is empty, needs population) -->
From registry.json:
```json
{
  "$schema": "https://shadcn-svelte.com/schema/registry.json",
  "name": "@uinstinct/svelte-wheel-picker",
  "homepage": "https://github.com/uinstinct/svelte-wheel-picker",
  "items": []
}
```

<!-- Existing CSS custom properties from +page.svelte (the ONLY color tokens allowed) -->
From src/routes/+page.svelte:
```css
--color-bg: #ffffff;       /* dark: #0f0f0f */
--color-text: #111111;     /* dark: #eeeeee */
--color-text-muted: #555555; /* dark: #aaaaaa */
--color-surface: #f4f4f5;  /* dark: #1c1c1e */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate registry.json and build registry output</name>
  <files>registry.json, static/r/wheel-picker.json</files>
  <read_first>
    - registry.json (current scaffold with empty items array)
    - package.json (confirm registry:build script exists)
    - src/lib/WheelPicker.svelte (confirm file exists at expected path)
    - src/lib/WheelPickerWrapper.svelte (confirm file exists)
    - src/lib/types.ts (confirm file exists)
    - src/lib/use-wheel-physics.svelte.ts (confirm file exists)
    - src/lib/use-controllable-state.svelte.ts (confirm file exists)
    - src/lib/use-typeahead-search.svelte.ts (confirm file exists)
    - src/lib/wheel-physics-utils.ts (confirm file exists)
  </read_first>
  <action>
    Replace the contents of `registry.json` with the complete registry manifest per decisions D-01 through D-05:

    ```json
    {
      "$schema": "https://shadcn-svelte.com/schema/registry.json",
      "name": "@uinstinct/svelte-wheel-picker",
      "homepage": "https://github.com/uinstinct/svelte-wheel-picker",
      "items": [
        {
          "name": "wheel-picker",
          "type": "registry:component",
          "title": "Wheel Picker",
          "description": "iOS-style wheel picker for Svelte 5 with inertia scrolling, infinite loop, and keyboard navigation.",
          "files": [
            { "path": "src/lib/WheelPicker.svelte", "type": "registry:component" },
            { "path": "src/lib/WheelPickerWrapper.svelte", "type": "registry:component" },
            { "path": "src/lib/types.ts", "type": "registry:lib" },
            { "path": "src/lib/use-wheel-physics.svelte.ts", "type": "registry:lib" },
            { "path": "src/lib/use-controllable-state.svelte.ts", "type": "registry:lib" },
            { "path": "src/lib/use-typeahead-search.svelte.ts", "type": "registry:lib" },
            { "path": "src/lib/wheel-physics-utils.ts", "type": "registry:lib" }
          ]
        }
      ]
    }
    ```

    Key constraints per locked decisions:
    - ONE registry item named `wheel-picker` (D-01) — no separate items
    - All 7 files listed (D-02) — no omissions
    - `.svelte` files use `registry:component`, all `.ts`/`.svelte.ts` use `registry:lib` (D-03)
    - Item-level type is `registry:component` (D-05)
    - No `registryDependencies` field needed (D-01 resolves STATE.md concern)

    After writing the file, run the build:
    ```bash
    pnpm run registry:build
    ```

    Verify the output file `static/r/wheel-picker.json` exists and contains content for all 7 source files.
  </action>
  <verify>
    <automated>test -f static/r/wheel-picker.json && node -e "const f=require('fs').readFileSync('static/r/wheel-picker.json','utf8'); const j=JSON.parse(f); const names=['WheelPicker.svelte','WheelPickerWrapper.svelte','types.ts','use-wheel-physics.svelte.ts','use-controllable-state.svelte.ts','use-typeahead-search.svelte.ts','wheel-physics-utils.ts']; const content=JSON.stringify(j); const found=names.filter(n=>content.includes(n)); if(found.length===7){console.log('PASS: all 7 files present')}else{console.log('FAIL: only '+found.length+'/7');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - registry.json contains `"name": "wheel-picker"` at items[0]
    - registry.json contains `"type": "registry:component"` at items[0]
    - registry.json items[0].files has exactly 7 entries
    - registry.json items[0].files contains `"path": "src/lib/WheelPicker.svelte"` with `"type": "registry:component"`
    - registry.json items[0].files contains `"path": "src/lib/types.ts"` with `"type": "registry:lib"`
    - registry.json items[0].files contains `"path": "src/lib/use-wheel-physics.svelte.ts"` with `"type": "registry:lib"`
    - static/r/wheel-picker.json exists (non-empty file)
    - static/r/wheel-picker.json contains string "WheelPicker" (file content was inlined)
    - `pnpm run registry:build` exits with code 0
  </acceptance_criteria>
  <done>registry.json has the complete wheel-picker item with all 7 files, registry:build succeeds, and static/r/wheel-picker.json is generated with inlined file contents</done>
</task>

<task type="auto">
  <name>Task 2: Add hero section to demo page per UI-SPEC</name>
  <files>src/routes/+page.svelte</files>
  <read_first>
    - src/routes/+page.svelte (current demo page — must see existing structure before modifying)
    - .planning/phases/06-shadcn-registry-and-demo-site/06-UI-SPEC.md (approved visual/interaction contract)
    - .planning/phases/06-shadcn-registry-and-demo-site/06-CONTEXT.md (locked decisions D-06 through D-08)
  </read_first>
  <action>
    Modify `src/routes/+page.svelte` to prepend a hero section above the four existing example sections. The existing `<script>`, `<section>` blocks, and `:global()` styles must remain UNCHANGED.

    **HTML changes** — Replace the existing `<h1>Wheel Picker</h1>` (line 41-42 area) with the hero section. The hero goes inside `<main>`, before the first `<section>`:

    ```html
    <section class="hero">
      <h1>svelte-wheel-picker</h1>
      <p class="hero-description">iOS-style wheel picker for Svelte 5. Smooth inertia scrolling, infinite loop, keyboard navigation.</p>
      <div class="install-block">
        <pre><code>npm install @uinstinct/svelte-wheel-picker</code></pre>
        <pre><code>npx shadcn-svelte@latest add https://svelte-wheel-picker.vercel.app/r/wheel-picker.json</code></pre>
      </div>
      <p class="install-note">(URL shown after deployment — update before publishing)</p>
    </section>
    ```

    Copy text is exact per UI-SPEC Copywriting Contract:
    - Title: `svelte-wheel-picker`
    - Description: `iOS-style wheel picker for Svelte 5. Smooth inertia scrolling, infinite loop, keyboard navigation.`
    - npm command: `npm install @uinstinct/svelte-wheel-picker`
    - shadcn command: `npx shadcn-svelte@latest add https://svelte-wheel-picker.vercel.app/r/wheel-picker.json`
    - Placeholder note: `(URL shown after deployment — update before publishing)`

    **CSS changes** — Add hero styles and update the existing `h1` rule. In the `<style>` block:

    1. Change the existing `h1` rule from `font-size: 20px` to `font-size: 28px` (UI-SPEC Display role, Pitfall 4 from RESEARCH.md). Keep `font-weight: 600`, `line-height: 1.2`, `margin-bottom: 16px` (changed from 24px to 16px per UI-SPEC spacing).

    2. Add these NEW CSS rules (all values from UI-SPEC and RESEARCH.md Pattern 3):

    ```css
    .hero {
      padding: 48px 0;
      margin-bottom: 24px;
    }

    .hero-description {
      font-size: 16px;
      font-weight: 400;
      line-height: 1.5;
      color: var(--color-text-muted);
      margin-bottom: 16px;
    }

    .install-block {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin-bottom: 8px;
    }

    .install-block pre {
      background: var(--color-surface);
      border-radius: 6px;
      padding: 8px 12px;
      overflow-x: auto;
      margin: 0;
    }

    .install-block code {
      font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
      font-size: 14px;
      color: var(--color-text-muted);
    }

    .install-note {
      font-size: 12px;
      font-style: italic;
      color: var(--color-text-muted);
      margin-bottom: 0;
    }
    ```

    **Anti-patterns to avoid (per RESEARCH.md and UI-SPEC):**
    - Do NOT use accent blue (`rgba(59,130,246,...)`) on any hero element — reserved for `[data-swp-selection]` only
    - Do NOT introduce new CSS custom properties — use only `--color-bg`, `--color-text`, `--color-text-muted`, `--color-surface`
    - Do NOT modify existing `<section>` blocks or their styles
    - Do NOT add click-to-copy JS — plain `<pre>/<code>` only per Interaction Contract
    - Ensure `overflow-x: auto` on `.install-block pre` to prevent mobile horizontal scroll (Pitfall 3)

    After making changes, run the existing test suite to confirm no regressions:
    ```bash
    PLAYWRIGHT_BROWSERS_PATH=.playwright pnpm test --run --project unit
    ```
  </action>
  <verify>
    <automated>grep -q 'svelte-wheel-picker' src/routes/+page.svelte && grep -q 'install-block' src/routes/+page.svelte && grep -q 'hero-description' src/routes/+page.svelte && grep -q 'npx shadcn-svelte' src/routes/+page.svelte && grep -q 'font-size: 28px' src/routes/+page.svelte && grep -q 'overflow-x: auto' src/routes/+page.svelte && grep -q 'install-note' src/routes/+page.svelte && echo "PASS: all hero elements present" || echo "FAIL: missing hero elements"</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/+page.svelte contains `<h1>svelte-wheel-picker</h1>`
    - src/routes/+page.svelte contains `<p class="hero-description">`
    - src/routes/+page.svelte contains `<div class="install-block">`
    - src/routes/+page.svelte contains `npm install @uinstinct/svelte-wheel-picker`
    - src/routes/+page.svelte contains `npx shadcn-svelte@latest add https://svelte-wheel-picker.vercel.app/r/wheel-picker.json`
    - src/routes/+page.svelte contains `<p class="install-note">`
    - src/routes/+page.svelte contains `font-size: 28px` in the h1 rule (not 20px)
    - src/routes/+page.svelte contains `overflow-x: auto` in install-block pre rule
    - src/routes/+page.svelte contains `.hero-description` CSS rule with `color: var(--color-text-muted)`
    - src/routes/+page.svelte contains `.install-block code` rule with `font-family: ui-monospace`
    - src/routes/+page.svelte does NOT contain `rgba(59, 130, 246` outside of existing `[data-swp-selection]` rules
    - The four existing `<section>` blocks (Single Wheel, Disabled Options, Infinite Loop, Two Wheels) remain intact
    - `PLAYWRIGHT_BROWSERS_PATH=.playwright pnpm test --run --project unit` passes (no regressions)
  </acceptance_criteria>
  <done>Demo page has a hero section with component title at 28px, one-line description, npm install and shadcn add commands in styled code blocks, placeholder note, and all four existing examples remain unchanged below</done>
</task>

</tasks>

<verification>
1. `pnpm run registry:build` exits 0 and produces `static/r/wheel-picker.json`
2. The built JSON references all 7 source files
3. `src/routes/+page.svelte` has hero section with correct copy per UI-SPEC
4. Hero uses only existing CSS custom properties (no new tokens)
5. All existing demo sections are unchanged
6. `PLAYWRIGHT_BROWSERS_PATH=.playwright pnpm test --run --project unit` passes
7. `pnpm build` succeeds (svelte-package still works)
</verification>

<success_criteria>
- DIST-04: registry.json populated per D-01 through D-05, static/r/wheel-picker.json generated by registry:build
- DIST-05: Demo page shows hero section with title, description, install commands, plus all four interactive examples below
- No regressions in existing unit tests
- No new runtime dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/06-shadcn-registry-and-demo-site/06-01-SUMMARY.md`
</output>
