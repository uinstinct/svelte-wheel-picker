---
phase: 06-shadcn-registry-and-demo-site
plan: "01"
subsystem: ui
tags: [shadcn-svelte, registry, demo-site, svelte5]

# Dependency graph
requires:
  - phase: 05-wheelpickerwrapper-and-package
    provides: All 7 source files in src/lib/ (WheelPicker.svelte, WheelPickerWrapper.svelte, types.ts, use-wheel-physics.svelte.ts, use-controllable-state.svelte.ts, use-typeahead-search.svelte.ts, wheel-physics-utils.ts)
provides:
  - registry.json populated with single wheel-picker item referencing all 7 source files
  - static/r/wheel-picker.json generated by registry:build (inlined file content for shadcn CLI install)
  - static/r/index.json generated by registry:build
  - Demo page hero section with title, description, npm and shadcn install commands
affects: [deployment, distribution, DIST-04, DIST-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Registry item collects all lib files into one installable unit — no registryDependencies needed between .svelte and .ts files in the same item"
    - "shadcn-svelte v1.2.3 requires registryDependencies field as empty array even when unused"

key-files:
  created:
    - registry.json (populated with wheel-picker item and 7 files)
    - static/r/wheel-picker.json (generated build artifact)
    - static/r/index.json (generated build artifact)
  modified:
    - src/routes/+page.svelte (hero section prepended, h1 font-size updated)

key-decisions:
  - "shadcn-svelte v1.2.3 requires registryDependencies field as empty array — schema validation fails if field is absent"
  - "Single registry item bundles all 7 source files — no separate items for WheelPicker vs WheelPickerWrapper"

patterns-established:
  - "Hero section uses only the four existing CSS custom properties (--color-bg, --color-text, --color-text-muted, --color-surface)"
  - "Install block uses plain pre/code with overflow-x auto — no click-to-copy JS"

requirements-completed: [DIST-04, DIST-05]

# Metrics
duration: 2min
completed: 2026-03-25
---

# Phase 6 Plan 1: shadcn-svelte Registry and Demo Site Hero Summary

**shadcn-svelte registry manifest with 7 inlined source files and demo page hero section with npm + shadcn install commands**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-25T04:01:30Z
- **Completed:** 2026-03-25T04:02:56Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Populated registry.json with a single `wheel-picker` item containing all 7 source files (2 components, 5 lib files with correct type annotations)
- Built static/r/wheel-picker.json via `pnpm run registry:build` — ready for `npx shadcn-svelte@latest add` installs
- Added hero section to demo page with title, description, and both install commands in styled code blocks
- Hero uses only existing CSS custom properties — no new tokens introduced

## Task Commits

Each task was committed atomically:

1. **Task 1: Populate registry.json and build registry output** - `0dbd9dd` (feat)
2. **Task 2: Add hero section to demo page per UI-SPEC** - `3d1d9ce` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified

- `registry.json` - shadcn-svelte registry manifest with wheel-picker item and 7 source file entries
- `static/r/wheel-picker.json` - Built registry JSON with inlined file content (generated artifact)
- `static/r/index.json` - Registry index (generated artifact)
- `src/routes/+page.svelte` - Hero section prepended above demo examples; h1 updated to 28px

## Decisions Made

- Added `"registryDependencies": []` to registry item — shadcn-svelte v1.2.3 schema validation (ZodError) requires this field to be present as an array even when empty. Plan decision D-01 noted no registryDependencies were needed but did not anticipate the schema requiring the field explicitly.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added required `registryDependencies: []` field to registry item**
- **Found during:** Task 1 (Populate registry.json and build registry output)
- **Issue:** `pnpm run registry:build` failed with ZodError: `"expected array"` at `items[0].registryDependencies` — the field is required by the schema even when empty
- **Fix:** Added `"registryDependencies": []` to the registry item in registry.json
- **Files modified:** registry.json
- **Verification:** `pnpm run registry:build` exits 0, static/r/wheel-picker.json generated with all 7 files
- **Committed in:** `0dbd9dd` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — schema requirement not anticipated by plan)
**Impact on plan:** Necessary for registry:build to succeed. No scope creep.

## Issues Encountered

- Pre-existing `types.test.ts` Vitest failure (No test suite found — file contains only compile-time type assertions, no `describe`/`test` blocks). 50 real unit tests all pass. Not a regression from this plan.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- DIST-04 complete: registry.json + static/r/wheel-picker.json ready for shadcn CLI installs
- DIST-05 complete: demo page with hero section ready for deployment
- Phase 6 (the only phase) is now complete — project is at v1.0 milestone
- Remaining items before publish: update shadcn install URL in hero once deployed to vercel.app

---
*Phase: 06-shadcn-registry-and-demo-site*
*Completed: 2026-03-25*
